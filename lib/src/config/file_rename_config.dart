class FileRenameConfig {
  const FileRenameConfig({
    required this.inputDir,
    required this.inputName,
    required this.outputName,
  });
  final String inputDir;
  final String inputName;
  final String outputName;
}
